# Explicitly define providers to avoid auto-detection issues (e.g. Node)
providers = ["python"]

# Deixamos o Nixpacks configurar o Python e Pip automaticamente
# (Removido phases.setup e phases.install manuais que estavam quebrando o pip)

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT"
